{
    "name": "music-player-frontend",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "react-scripts": "4.0.3",
        "lucide-react": "^0.112.0"
    },
    "scripts": {
        "start": "NODE_OPTIONS='--openssl-legacy-provider --no-warnings' react-scripts start",
        "build": "NODE_OPTIONS='--openssl-legacy-provider --no-warnings' CI=false react-scripts build"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}